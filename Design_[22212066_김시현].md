# Bomb-Tag - Design Document

**Project Title** : Bomb-Tag (나만 아니면 돼~)
**Student Info** : 22212066, 김시현, rlatlgus127@gmail.com

## [ Revision history ]

| Revision date | Version # | Description | Author |
| :--- | :--- | :--- | :--- |
| 05/26/2026 | 0.03 | First Draft (Class, Sequence, State Machine) | 김시현 |

---

## = Contents =

1. Introduction
2. Class diagram
3. Sequence diagram
4. State machine diagram
5. Implementation requirements
6. Glossary
7. References

---

## 1. Introduction (도입)

### 1.1 Summary

* ** 복잡한 시스템과 긴 플레이 타임을 요구하는 하드코어 게임에서 벗어나, 현대인들이 짧은 시간 동안 직관적으로 즐길 수 있는 스낵 컬처형 게임 <b>‘Bomb-Tag(나만 아니면 돼~)’</b>이다.**

### 1.2 게임소개 (Game Introduction)

* **‘Bomb-Tag(나만 아니면 돼~)’는 오프라인의 ‘폭탄 돌리기’ 규칙을 온라인 실시간 멀티플레이어 환경으로 이식한 하이퍼 캐주얼 파티 게임이다.**

* **핵심 메커니즘: 제한 시간 내에 자신의 폭탄을 다른 플레이어에게 넘겨야 하는 원초적인 긴장감을 제공하며, 실시간 추격과 충돌이라는 게임의 본질적인 재미에 집중한다.**

* **활용도: 지인들과의 가벼운 내기나 모임에서의 아이스브레이킹을 위한 매개체로 활용될 수 있도록 설계되었다.**

### 1.3 Goals

* **실시간 네트워크 환경 구축: 플레이어 간의 이동 위치와 폭탄 소유 상태가 지연 없이 동기화되는 안정적인 멀티플레이 환경을 구현한다.**

* **직관적이고 빠른 템포의 게임성 제공: 별도의 학습 없이 즉시 플레이할 수 있도록 규칙을 단순화하고, 3~5분 내외의 짧은 플레이 타임을 통해 속도감 있는 경험을 제공한다.**

* **공정하고 정확한 판정 시스템: 서버 권한(Server Authority)을 기반으로 플레이어 간의 충돌(폭탄 양도) 및 제한 시간 만료 시점의 판정을 정확하게 처리하여 승패의 신뢰도를 확보한다.**

---

## 2. Class diagram

시스템을 구성하는 핵심 객체들의 정적인 구조와 속성, 메서드를 정의한다. 리슨 서버(Listen Server) 환경에서의 권한 관리를 위해 서버 권한 객체와 클라이언트 뷰포트 객체를 분리하였다. UI부분은 클래스 다이어그램을 복잡하게만 할 뿐이여서 현 부분에선 제외했다.

<img width="750" height="500" alt="Image" src="https://github.com/user-attachments/assets/ab7c5502-6857-44b1-9823-48e034c53614" />

### 2.1 System & Lobby Management
게임 시작 전 유저의 세션을 유지하고 방 목록 및 매치메이킹을 관리하는 시스템 클래스 그룹이다.

#### 2.1.1 MatchmakingManager
| 구분 | Name | Type | Description |
| :--- | :--- | :--- | :--- |
| **Attribute** | RoomList | List\<Room\> | 현재 서버에 생성되어 대기 중인 방 객체들의 리스트 |
| **Operation** | GetRoomList() | List\<Room\> | 클라이언트에게 현재 유효한 방 목록을 반환 |
| **Operation** | CreateRoom() | Room | 새로운 방 객체를 생성하고 초기화 |
| **Operation** | JoinRoom() | bool | 특정 방의 수용 인원을 체크하여 입장 가능 여부 반환 |

#### 2.1.2 Room / Player
| Class | Name | Type | Description |
| :--- | :--- | :--- | :--- |
| **Room** | RoomName | String | 대기열에 표시될 방의 고유 이름 |
| **Room** | MaxPlayers | int | 방의 최대 수용 인원 (예: 8인) |
| **Room** | StartGame() | void | 방장이 시작 버튼 클릭 시 인게임 세션으로 전환 |
| **Player** | Nickname | String | 사용자가 설정한 고유 닉네임 |
| **Player** | bIsHost | bool | 현재 방의 마스터 권한 소지 여부 |

#### 2.1.3 BombTag_GameInstance
| 구분 | Name | Type | Description |
| :--- | :--- | :--- | :--- |
| **Attribute** | StoredNickname | String | 맵 이동 시 소멸되지 않고 유지되는 유저 닉네임 정보 |
| **Attribute** | UserSessionToken | String | 서버 식별용 고유 세션 토큰 |
| **Operation** | SaveUserInfo() | void | 로비에서 설정된 정보를 메모리에 임시 저장 |
| **Operation** | LoadUserInfo() | String | 인게임 맵 로딩 완료 후 저장된 닉네임을 복원 |

---

### 2.2 Game Framework (Core Logic)
언리얼 엔진의 멀티플레이어 핵심 로직과 네트워크 동기화를 담당하는 클래스 그룹이다.

#### 2.2.1 BombTag_GameMode (Server Only)
| 구분 | Name | Type | Description |
| :--- | :--- | :--- | :--- |
| **Attribute** | RoundTime | float | 한 라운드의 총 제한 시간 |
| **Attribute** | CurrentRound | int | 현재 진행 중인 라운드 번호 |
| **Operation** | StartMatch() | void | 플레이어 스폰 완료 후 서버 타이머를 가동 |
| **Operation** | EndMatch() | void | 라운드 종료 판정 및 결과 리포트 생성 |
| **Operation** | RespawnPlayers() | void | 새로운 라운드 시작 시 모든 플레이어를 아레나로 재배치 |
| **Operation** | ValidateBombTransfer() | bool | 두 캐릭터 충돌 시 거리 계산을 통한 전달 유효성 검증 |

#### 2.2.2 BombTag_GameState & PlayerState (Sync)
| Class | Name | Type | Description |
| :--- | :--- | :--- | :--- |
| **GameState** | RemainingTime | float | 전체 유저에게 동기화될 남은 시간 정보 |
| **GameState** | AlivePlayerCount | int | 현재 생존 인원 실시간 카운트 |
| **PlayerState** | PlayerScore | int | 개별 플레이어의 누적 승리 횟수 (Replicated) |
| **PlayerState** | Ping | int | 네트워크 지연 속도 모니터링 데이터 |
| **PlayerState** | bIsEliminated | bool | 현재 플레이어의 탈락 여부 동기화 변수 |

#### 2.2.3 BombTag_PlayerController
| 구분 | Name | Type | Description |
| :--- | :--- | :--- | :--- |
| **Attribute** | PlayerHUD | UserWidget | 유저 화면에 렌더링되는 메인 UI 인터페이스 |
| **Operation** | SetupInputComponent() | void | 키보드/마우스 입력을 캐릭터 이동 로직과 바인딩 |
| **Operation** | UpdateHUD() | void | GameState로부터 동기화된 데이터를 UI에 반영 |

---

### 2.3 Gameplay Objects
실제 아레나 맵 내에서 상호작용하는 물리 객체 및 캐릭터 클래스 그룹이다.

#### 2.3.1 BombTag_Character
| 구분 | Name | Type | Description |
| :--- | :--- | :--- | :--- |
| **Attribute** | bIsBomber | bool | 현재 술래인지 판별하는 동기화 변수 |
| **Attribute** | MoveSpeed | float | 캐릭터의 이동 속도 가중치 |
| **Attribute** | TargetBoomSocket | String | 폭탄 모델이 부착될 스켈레탈 메시의 소켓 명칭 |
| **Operation** | MoveForward / Right | void | 플레이어의 축 입력에 따른 이동 처리 |
| **Operation** | JumpAction() | void | 캐릭터의 수직 점프 동작 실행 |
| **Operation** | Server_RequestPassBomb() | void | 충돌 시 서버에 소유권 이전을 요청하는 RPC 호출 |

#### 2.3.2 BombItem & Item
| Class | Name | Type | Description |
| :--- | :--- | :--- | :--- |
| **BombItem** | TickTockSound | SoundCue | 시간이 흐름에 따라 긴박함을 유도하는 효과음 |
| **BombItem** | ExplosionRadius | float | 폭발 시 영향을 주는 반경 범위 |
| **BombItem** | Multicast_ShowExplosion()| void | 모든 클라이언트에 폭발 이펙트 동시 재생 명령 |
| **Item** | EffectType / Duration | int / float | 소모성 아이템의 종류 및 지속 시간 |
| **Item** | ApplyEffect() | void | 충돌한 캐릭터에게 능력치 버프 부여 |

#### 2.3.3 Arena
| 구분 | Name | Type | Description |
| :--- | :--- | :--- | :--- |
| **Attribute** | SpawnPoint_List | List\<Vector\> | 맵 내에 사전에 정의된 안전 스폰 좌표 목록 |
| **Operation** | GetRandomSpawnPoint() | Vector | 중복되지 않는 무작위 스폰 위치를 산출하여 반환 |
---

### 3.1 Sequence diagram

#### 3.1. UC #2: Create Game Session (방 만들기)

<img width="750" height="500" alt="Image" src="https://github.com/user-attachments/assets/5fd16531-831f-47fe-9b39-7cef79882f3a" />

이 시퀀스 다이어그램은 호스트(방장)가 게임 로비에서 새로운 방을 생성하는 과정을 보여줍니다. 
언리얼 엔진의 클라이언트-서버 구조를 반영하여, 클라이언트가 UI에서 방 생성을 시도하면 `Server RPC`를 통해 서버의 Lobby 객체에 요청(`Server_CreateRoom`)을 보냅니다. 
서버는 내부적으로 유효성을 검사한 후 `GameManager`를 통해 실제 게임 세션을 할당하며, 생성이 완료되면 `Client RPC`를 통해 클라이언트에게 성공 여부와 Session ID를 반환하여 방 진입을 처리합니다.

#### 3.2. UC #3 & #4: Join Room & Game Ready (방 입장 및 준비)

<img width="750" height="600" alt="Image" src="https://github.com/user-attachments/assets/f5ee781f-acf2-4473-9a31-8b22620fa163" />

이 시퀀스 다이어그램은 다른 플레이어(Guest)가 생성된 방에 입장하고, 게임 시작 조건을 충족하는 과정을 나타냅니다.
플레이어가 방에 접속하면 서버는 이를 확인하여 리턴하고, 이후 `loop` 구문 내에서 각 플레이어의 `Server_PlayerReady` RPC 호출을 대기합니다. 
서버의 Lobby 객체는 `CheckAllReady` 함수를 통해 모든 접속자의 준비 상태를 확인하며, 모든 인원이 준비를 마치면 비로소 `GameSession` 객체를 호출하여 게임 인게임(In-game) 환경을 초기화(Initialize)합니다.

#### 3.3. UC #6: Movement & Initial Core Game Loop (이동 및 초기 게임 루프)

<img width="750" height="500" alt="Image" src="https://github.com/user-attachments/assets/89ac2b59-0ad7-47e0-879e-84d077f8421d" />

이 다이어그램은 게임 세션이 본격적으로 시작된 후, 플레이어들의 이동과 위치 동기화가 이루어지는 핵심 루프(Core Loop)를 나타냅니다. 
게임 시작 시 서버는 `BombManager`를 통해 무작위로 최초의 폭탄 소유자를 지정합니다. 이후 `loop` 구간 내에서 각 클라이언트는 언리얼 엔진의 Character Movement 컴포넌트(또는 `Server RPC`)를 통해 자신의 이동 좌표를 서버에 지속적으로 전달합니다. 
서버(`GameSession`)는 클라이언트들의 위치를 취합 및 검증한 뒤, `Replication`을 통해 모든 접속자에게 현재 위치를 브로드캐스트하여 화면 끊김 없는 동기화를 보장합니다.

#### 3.4. UC #7: Bomb Tagging / Pass Logic (폭탄 전달 로직)

<img width="750" height="500" alt="Image" src="https://github.com/user-attachments/assets/0606141d-5567-4d3c-b392-749fcfb704b6" />

이 시스템은 Bomb-Tag의 가장 핵심적인 상호작용인 '폭탄 전달' 과정을 상세히 보여줍니다. 네트워크 지연(Ping)으로 인한 클라이언트 간의 억울한 판정을 방지하기 위해 **서버 권한(Server Authority)** 구조를 엄격하게 따릅니다.

1. 현재 폭탄 소유자(Player1)가 충돌을 감지하여 `Server_PassBomb` RPC를 호출합니다.
2. 서버의 `BombManager`는 두 플레이어 사이의 실제 거리와 무적 시간(쿨타임) 등을 서버 기준으로 재계산하여 유효성(`ValidateTag`)을 판정합니다.
3. **[Case 1: 성공]** 판정이 유효하다면 서버 상태를 업데이트합니다. 이때 언리얼 엔진의 `RepNotify` 기능을 활용하여 변수 동기화 시 자동으로 각 클라이언트의 시각적 효과(폭탄 모델링 이동, UI 변경 등)가 처리되도록 설계했습니다.
4. **[Case 2: 실패]** 거리가 너무 멀거나 무효한 판정일 경우, 서버는 전달을 거부하고 요청한 클라이언트에게 실패를 반환합니다.

#### 3.5. UC: Bomb Explode & Game Over (폭탄 폭발 및 게임 종료)

<img width="750" height="500" alt="Image" src="https://github.com/user-attachments/assets/3335cb30-7e3d-4f9e-99ae-02c352c68f66" />

이 시퀀스 다이어그램은 폭탄의 타이머가 종료되었을 때 서버가 이를 감지하고, 플레이어를 탈락시킨 후 게임 종료 여부를 판단하는 과정을 나타냅니다.

- **Trigger:** 폭탄 객체의 타이머 만료 (OnTimerEnd).
- **Process:** 1. 서버(GameMode)는 폭탄을 들고 있던 플레이어에게 탈락(Client RPC) 명령을 내립니다.
  2. 서버는 GameState의 생존자 수를 업데이트합니다.
  3. **[Alt] 생존자 수 조건:**
     - 생존자가 1명 이하일 경우: 게임 상태를 GameOver로 변경하고, 모든 클라이언트에게 결과창 UI를 띄웁니다(Multicast RPC).
     - 생존자가 2명 이상일 경우: 다음 라운드를 위해 새로운 폭탄을 스폰하고 게임을 계속 진행합니다.

#### 3.6. UC: Item Pickup & Immediate Use (아이템 획득 및 즉시 사용)

<img width="750" height="500" alt="Image" src="https://github.com/user-attachments/assets/774fa0a3-d203-4d01-86b3-f1349291e84b" />

이 시퀀스 다이어그램은 맵에 생성된 아이템을 플레이어가 획득했을 때, 서버를 거쳐 효과가 적용되고 모든 클라이언트에서 아이템이 동기화되어 사라지는 과정을 나타냅니다.

- **Trigger:** 플레이어 캐릭터가 아이템 객체와 충돌(Overlap)함.
- **Process:** 1. 클라이언트(Player)는 아이템과 충돌 시, 서버(GameMode)에 아이템 획득을 요청합니다 (Server RPC).
  2. 서버는 해당 아이템이 아직 맵에 유효하게 존재하는지 검증(Validate)합니다.
  3. **[Alt] 아이템 유효성 확인 (IsValid == true):**
     - 서버는 아이템을 획득한 플레이어에게 버프 효과(예: 이동속도 증가)를 적용합니다.
     - 서버는 모든 클라이언트에게 해당 아이템 객체를 파괴하라고 명령하여 화면에서 지웁니다 (Multicast RPC).
     - 획득한 클라이언트의 UI를 업데이트하여 버프 아이콘을 표시합니다.

## 4. State diagram

<img width="750" height="600" alt="Image" src="https://github.com/user-attachments/assets/f64f8e95-0421-405e-9eba-ec1c8b4549d2" />

유저가 게임을 시작하면 로그인을 할 것인지 게스트로 참여를 할 것인지 선택한다. 선택 후 성공적으로 처리되면 Lobby로 온다. Lobby에서 방을 만들 지 참여할 지 선택한다. WaitingRoom에서 방장이 GameStart를 하면 In-Game으로 넘어간다. 유저들은 각각 자신의 플레이어가 생성되고 움직임에 해당되는 버튼을 누름으로 플레이어를 이동할 수 있다. 랜덤적으로 한 명에게 폭탄을 부여하고 플레이어가 상호작용을 통해 폭탄을 전달할 수 있다. 일정 시간이 지나 폭탄이 폭발하게 되면 플레이어는 죽게 된다. 죽었을 경우 관전모드로 넘어가게 되고 만약 살아남은 생존자가 2명 미만이라면 게임이 종료된다.  종료 후 다시 WaitingRoom으로 돌아오고 방장이 다시 시작을 할 수 있게 된다. 유저는 방을 나가면 다시 로비로 돌아오게 되고 게임 종료 시 클라이언트가 종료 된다.

## 5. Implementation requirements

본 프로젝트(Bomb-Tag)를 원활하게 개발하고 구동하기 위한 하드웨어 및 소프트웨어 요구사항은 다음과 같다.

### 5.1 H/W platform requirements
* **CPU**: Intel Core i5 / AMD Ryzen 5 이상
* **RAM**: 8GB 이상 (언리얼 엔진 에디터 구동을 위해 16GB 권장)
* **GPU**: NVIDIA GTX 1060 이상 (언리얼 5 라이팅 및 멀티플레이 구동용)
* **Storage**: 10GB 이상의 여유 공간 (SSD 권장)
* **Network**: 실시간 멀티플레이 통신을 위한 인터넷 연결 필수

### 5.2 S/W platform requirements
* **OS**: Windows 10 / 11 (64-bit)
* **Game Engine**: Unreal Engine 5.7.1
* **IDE**: Visual Studio 2022 (C++ 컴파일 및 디버깅)
* **Network Subsystem**: Unreal Engine Online Subsystem (Null 또는 Steam)


## 6. Glossary

| 용어 (Term) | 설명 (Description) |
| :--- | :--- |
| **Unreal Engine 5** | 고품질 그래픽과 자체 네트워크 프레임워크를 기본으로 제공하는 글로벌 게임 개발 엔진. |
| **Listen Server** | 방을 생성한 호스트(Host)가 게임 서버의 역할과 클라이언트의 역할을 동시에 수행하는 멀티플레이 아키텍처. |
| **RPC (Remote Procedure Call)** | 클라이언트와 서버 간에 서로의 함수를 원격으로 호출하여 네트워크 데이터를 동기화하는 통신 기법. |
| **Composite State (복합 상태)** | UML 상태 머신 다이어그램에서, 내부에 하위 상태(Sub-state)들을 포함하고 있는 큰 범위의 상위 상태. |
| **Tagging (태깅)** | 게임 내에서 플레이어 간 충돌(Collision)을 통해 폭탄의 소유권을 다른 플레이어에게 넘기는 핵심 상호작용. |


## 7. References
* 강의 자료 : Structural Modeling, Behavior Modeling
* Unreal Engine 5 Official Documentation (https://docs.unrealengine.com)
